version: "3"

services:
  terra:
    image: terracotta
    container_name: MyTerra
    ports:
      - 8080:80
    volumes:
      - terra:/home
    working_dir: /home
    command: bash -c "service nginx restart &&
                      service nginx start"
    tty: true

volumes:
  terra:
    external: True